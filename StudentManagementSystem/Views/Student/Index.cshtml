@using StudentManagementSystem.Models.Enums
@model List<StudentManagementSystem.Models.ViewModels.Students.ListStudentViewModel>
<div class="content-medium">
<div class="table-card">
<h2>Students</h2>

<a asp-action="Create" class="btn btn-primary mb-3">
    Add Student
</a>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Full Name</th>
            <th>Status</th>
            <th>Actions</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var student in Model)
        {
            <tr>
                <td>@student.FullName</td>
                @{
                    var statusClass = student.Status switch
                    {
                        StudentStatus.Active => "bg-success",
                        StudentStatus.Inactive => "bg-danger",
                        StudentStatus.Graduated => "bg-primary",
                        _ => "bg-secondary"
                    };
                }

                <td>
                    <span class="badge @statusClass">
                        @student.Status
                    </span>
                </td>
                <td>
                    <a asp-action="Details"
                       asp-route-id="@student.Id"
                       class="btn btn-info btn-sm">
                        Details
                    </a>

                    <a asp-action="Edit"
                       asp-route-id="@student.Id"
                       class="btn btn-warning btn-sm">
                        Edit
                    </a>
                    <a asp-action="Delete"
                       asp-route-id="@student.Id"
                       class="btn btn-danger btn-sm">
                        Delete
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>
</div>